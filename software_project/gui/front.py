from tkinter import *

root = Tk()
root.title("picAmaze")

# #파일 프레임 (파일 추가, 선택 삭제)
# file_frame = Frame(root)
# file_frame.pack()
#
# btn_add_file = Button(file_frame, text='태그 추가')
# btn_add_file.pack(side= 'left')
#
# bnt
#
# root.resizable(False,False)
# root.mainloop()